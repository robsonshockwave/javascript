const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d //sintaxe conhecida como infix
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo, -soma)

// d++ postfix
// ++d prefix
// dd + d infix dois operadores diferentes
// -a operador unario